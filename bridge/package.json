{
  "name": "gas-news-bot-bridge",
  "version": "1.0.0",
  "description": "Slack signature verification bridge for GAS news bot",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "docker:build": "docker build -t gas-news-bot-bridge .",
    "docker:run": "docker run -p 8080:8080 --env-file .env gas-news-bot-bridge"
  },
  "keywords": [
    "slack",
    "gas",
    "news-bot",
    "cloud-run"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
